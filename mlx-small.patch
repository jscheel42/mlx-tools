diff --git a/mlx_lm/generate.py b/mlx_lm/generate.py
index ab0bdb8..5bcda7d 100644
--- a/mlx_lm/generate.py
+++ b/mlx_lm/generate.py
@@ -314,6 +314,7 @@ def generate_step(
     kv_group_size: int = 64,
     quantized_kv_start: int = 0,
     prompt_progress_callback: Optional[Callable[[int, int], None]] = None,
+    prefill_cache_callback: Optional[Callable[[Any], None]] = None,
     input_embeddings: Optional[mx.array] = None,
 ) -> Generator[Tuple[mx.array, mx.array], None, None]:
     """
@@ -446,6 +447,9 @@ def generate_step(
             )
             mx.clear_cache()
 
+        if prefill_cache_callback is not None:
+            prefill_cache_callback(prompt_cache)
+
         y, logprobs = _step(input_tokens=prompt, input_embeddings=input_embeddings)
 
     mx.async_eval(y, logprobs)
@@ -697,6 +701,7 @@ def stream_generate(
     else:
         kwargs.pop("max_kv_size", None)
         kwargs.pop("prompt_progress_callback", None)
+        kwargs.pop("prefill_cache_callback", None)
         token_generator = speculative_generate_step(
             prompt, model, draft_model, **kwargs
         )
@@ -850,6 +855,7 @@ class Batch:
     samplers: List[Any]
     logits_processors: List[Any]
     tokens: List[mx.array]
+    prefill_caches: Optional[List[List[Any]]] = None
 
     def __len__(self):
         return len(self.uids)
@@ -862,6 +868,8 @@ class Batch:
         self.samplers = [self.samplers[k] for k in keep_idx]
         self.logits_processors = [self.logits_processors[k] for k in keep_idx]
         self.tokens = [self.tokens[k] for k in keep_idx]
+        if self.prefill_caches is not None:
+            self.prefill_caches = [self.prefill_caches[k] for k in keep_idx]
         keep_idx = mx.array(keep_idx, mx.int32)
         self.y = self.y[keep_idx]
         for c in self.cache:
@@ -876,6 +884,10 @@ class Batch:
         self.samplers.extend(other.samplers)
         self.logits_processors.extend(other.logits_processors)
         self.tokens.extend(other.tokens)
+        if self.prefill_caches is not None and other.prefill_caches is not None:
+            self.prefill_caches.extend(other.prefill_caches)
+        elif self.prefill_caches is None:
+            self.prefill_caches = other.prefill_caches
         for c, o in zip(self.cache, other.cache):
             c.extend(o)
 
@@ -935,6 +947,7 @@ class BatchGenerator:
         logprobs: mx.array
         finish_reason: Optional[str]
         prompt_cache: Callable[[], List[Any]]
+        prompt_only_cache: Optional[List[Any]] = None
 
     def __init__(
         self,
@@ -1119,6 +1132,10 @@ class BatchGenerator:
             c.finalize()
         mx.clear_cache()
 
+        prefill_caches = [
+            [c.extract(i) for c in prompt_cache] for i in range(len(uids))
+        ]
+
         y, logprobs = self._step(
             inputs, prompt_cache, samplers, logits_processors, tokens
         )
@@ -1135,6 +1152,7 @@ class BatchGenerator:
             list(samplers),
             list(logits_processors),
             tokens,
+            prefill_caches,
         )
 
     def _step(
@@ -1245,6 +1263,7 @@ class BatchGenerator:
             zip(y, batch.uids, batch.num_tokens, batch.max_tokens)
         ):
             cache = None
+            prompt_only_cache = None
             num_tok += 1
             batch.num_tokens[e] = num_tok
             if t in self.stop_tokens:
@@ -1258,7 +1277,18 @@ class BatchGenerator:
                 keep_idx.append(e)
             if finish_reason is not None:
                 cache = batch.extract_cache(e)
-            responses.append(self.Response(uid, t, logprobs[e], finish_reason, cache))
+                if batch.prefill_caches is not None:
+                    prompt_only_cache = batch.prefill_caches[e]
+            responses.append(
+                self.Response(
+                    uid,
+                    t,
+                    logprobs[e],
+                    finish_reason,
+                    cache,
+                    prompt_only_cache,
+                )
+            )
 
         # Remove any finished completions
         if len(end_idx):
diff --git a/mlx_lm/server.py b/mlx_lm/server.py
index 36695aa..c5dd03c 100644
--- a/mlx_lm/server.py
+++ b/mlx_lm/server.py
@@ -183,7 +183,6 @@ def process_message_content(messages):
 
 
 class LRUPromptCache:
-
     @dataclass
     class CacheEntry:
         prompt_cache: List[Any]
@@ -287,14 +286,40 @@ class LRUPromptCache:
         )
 
     def fetch_nearest_cache(self, model, tokens):
+        total_tokens = len(tokens)
         result = self._search(model, tokens)
         if result.exact is not None:
             cache_entry = self._extract(result.model, result.exact)
+            if len(result.exact) > 0:
+                if can_trim_prompt_cache(cache_entry.prompt_cache):
+                    trim_prompt_cache(cache_entry.prompt_cache, 1)
+                    logging.debug(
+                        "[LRUPromptCache] hit=exact-adjusted reused=%d total=%d rest=1",
+                        len(result.exact) - 1,
+                        total_tokens,
+                    )
+                    return cache_entry.prompt_cache, tokens[-1:]
+                logging.debug(
+                    "[LRUPromptCache] hit=exact-unusable reason=not_trimmable total=%d",
+                    total_tokens,
+                )
+                return None, tokens
+            logging.debug(
+                "[LRUPromptCache] hit=exact reused=%d total=%d",
+                len(result.exact),
+                total_tokens,
+            )
             return cache_entry.prompt_cache, []
 
         if result.shorter is not None:
             cache_entry = self._extract(result.model, result.shorter)
             prefix_len = len(result.shorter)
+            logging.debug(
+                "[LRUPromptCache] hit=shorter reused=%d total=%d rest=%d",
+                prefix_len,
+                total_tokens,
+                total_tokens - prefix_len,
+            )
             return cache_entry.prompt_cache, tokens[prefix_len:]
 
         if result.longer is not None:
@@ -304,8 +329,19 @@ class LRUPromptCache:
                 prefix = min(len(tokens) - 1, result.common_prefix)
                 num_to_trim = len(result.longer) - prefix
                 trim_prompt_cache(cache, num_to_trim)
+                logging.debug(
+                    "[LRUPromptCache] hit=longer reused=%d total=%d rest=%d",
+                    prefix,
+                    total_tokens,
+                    total_tokens - prefix,
+                )
                 return cache, tokens[prefix:]
 
+        logging.debug(
+            "[LRUPromptCache] hit=miss reused=0 total=%d common_prefix=%d",
+            total_tokens,
+            result.common_prefix,
+        )
         return None, tokens
 
     def insert_cache(self, model, tokens, prompt_cache):
@@ -801,7 +837,7 @@ class ResponseGenerator:
 
                     ncaches, nbytes = len(self.prompt_cache), self.prompt_cache.nbytes
                     logging.info(
-                        f"We have {ncaches} kv caches that take {nbytes/1e9:.2f} GB"
+                        f"We have {ncaches} kv caches that take {nbytes / 1e9:.2f} GB"
                     )
 
                     (uid,) = batch_generator.insert(
@@ -902,8 +938,12 @@ class ResponseGenerator:
 
                         if r.finish_reason is not None:
                             result["rqueue"].put(None)
-                            self.prompt_cache.insert_cache(
-                                current_model_key, result["cache_key"], r.prompt_cache
+                            self._insert_prompt_caches(
+                                current_model_key,
+                                result["cache_key"],
+                                r.prompt_cache,
+                                len(result["ctx"].prompt),
+                                prompt_only_cache=r.prompt_only_cache,
                             )
                             del batch_results[r.uid]
 
@@ -920,8 +960,11 @@ class ResponseGenerator:
                             if uid not in batch_results:
                                 continue
                             result = batch_results[uid]
-                            self.prompt_cache.insert_cache(
-                                current_model_key, result["cache_key"], prompt_cache
+                            self._insert_prompt_caches(
+                                current_model_key,
+                                result["cache_key"],
+                                prompt_cache,
+                                len(result["ctx"].prompt),
                             )
                             del batch_results[uid]
 
@@ -937,6 +980,13 @@ class ResponseGenerator:
             model = self.model_provider.model
             tokenizer = self.model_provider.tokenizer
             draft_model = self.model_provider.draft_model
+            prompt_only_cache = None
+
+            def capture_prefill_cache(prefill_cache):
+                nonlocal prompt_only_cache
+                if prompt_only_cache is None:
+                    prompt_only_cache = copy.deepcopy(prefill_cache)
+                    logging.debug("[LRUPromptCache] prefill_capture=ok")
 
             # Prepare the prompt
             prompt = self._tokenize(tokenizer, request, args)
@@ -980,7 +1030,7 @@ class ResponseGenerator:
                     cache += make_prompt_cache(self.model_provider.draft_model)
 
             ncaches, nbytes = len(self.prompt_cache), self.prompt_cache.nbytes
-            logging.info(f"We have {ncaches} kv caches that take {nbytes/1e9:.2f} GB")
+            logging.info(f"We have {ncaches} kv caches that take {nbytes / 1e9:.2f} GB")
 
             # Process the prompt and generate tokens
             for gen in stream_generate(
@@ -994,6 +1044,7 @@ class ResponseGenerator:
                 draft_model=draft_model,
                 num_draft_tokens=args.num_draft_tokens,
                 prompt_progress_callback=progress,
+                prefill_cache_callback=capture_prefill_cache,
             ):
                 rqueue.put(
                     Response(
@@ -1016,8 +1067,12 @@ class ResponseGenerator:
             rqueue.put(None)
 
             # Save the KV cache again
-            self.prompt_cache.insert_cache(
-                self.model_provider.model_key, cache_key, cache
+            self._insert_prompt_caches(
+                self.model_provider.model_key,
+                cache_key,
+                cache,
+                len(prompt),
+                prompt_only_cache=prompt_only_cache,
             )
 
         except Exception as e:
@@ -1055,6 +1110,55 @@ class ResponseGenerator:
     def cli_args(self):
         return self.model_provider.cli_args
 
+    def _insert_prompt_caches(
+        self,
+        model_key,
+        cache_key,
+        prompt_cache,
+        prompt_len: int,
+        prompt_only_cache=None,
+    ):
+        if prompt_only_cache is not None:
+            prompt_only_len = max(0, prompt_len - 1)
+            prompt_only_key = cache_key[:prompt_only_len]
+            self.prompt_cache.insert_cache(
+                model_key, prompt_only_key, prompt_only_cache
+            )
+            logging.debug(
+                "[LRUPromptCache] prompt_only_insert=ok source=prefill_capture "
+                "prompt_len=%d cached_prefix_len=%d full_len=%d",
+                prompt_len,
+                prompt_only_len,
+                len(cache_key),
+            )
+            return
+
+        self.prompt_cache.insert_cache(model_key, cache_key, prompt_cache)
+
+        generated_tokens = len(cache_key) - prompt_len
+        if generated_tokens <= 0:
+            return
+
+        if not can_trim_prompt_cache(prompt_cache):
+            logging.debug(
+                "[LRUPromptCache] prompt_only_insert=skip reason=not_trimmable "
+                "prompt_len=%d full_len=%d",
+                prompt_len,
+                len(cache_key),
+            )
+            return
+
+        prompt_only_cache = copy.deepcopy(prompt_cache)
+        trimmed = trim_prompt_cache(prompt_only_cache, generated_tokens)
+        prompt_only_key = cache_key[:-generated_tokens]
+        self.prompt_cache.insert_cache(model_key, prompt_only_key, prompt_only_cache)
+        logging.debug(
+            "[LRUPromptCache] prompt_only_insert=ok trimmed=%d prompt_len=%d full_len=%d",
+            trimmed,
+            prompt_len,
+            len(cache_key),
+        )
+
 
 class APIHandler(BaseHTTPRequestHandler):
     def __init__(
@@ -1124,14 +1228,21 @@ class APIHandler(BaseHTTPRequestHandler):
 
         indent = "\t"  # Backslashes can't be inside of f-strings
         logging.debug(f"Incoming Request Body: {json.dumps(self.body, indent=indent)}")
-        assert isinstance(
-            self.body, dict
-        ), f"Request should be dict, but got {type(self.body)}"
+        assert isinstance(self.body, dict), (
+            f"Request should be dict, but got {type(self.body)}"
+        )
 
         # Extract request parameters from the body
         self.stream = self.body.get("stream", False)
         self.stream_options = self.body.get("stream_options", None)
         self.requested_model = self.body.get("model", "default_model")
+        self.response_model_id = self.requested_model
+        model_id = getattr(self.response_generator.cli_args, "model_id", None)
+        if model_id and self.requested_model == model_id:
+            self.requested_model = (
+                self.response_generator.cli_args.model or "default_model"
+            )
+            self.response_model_id = model_id
         self.requested_draft_model = self.body.get("draft_model", "default_model")
         self.num_draft_tokens = self.body.get(
             "num_draft_tokens", self.response_generator.cli_args.num_draft_tokens
@@ -1285,7 +1396,7 @@ class APIHandler(BaseHTTPRequestHandler):
             "id": self.request_id,
             "system_fingerprint": self.system_fingerprint,
             "object": self.object_type,
-            "model": self.requested_model,
+            "model": self.response_model_id,
             "created": self.created,
             "choices": [
                 {
@@ -1609,7 +1720,7 @@ class APIHandler(BaseHTTPRequestHandler):
             "id": self.request_id,
             "system_fingerprint": self.system_fingerprint,
             "object": "chat.completion",
-            "model": self.requested_model,
+            "model": self.response_model_id,
             "created": self.created,
             "choices": [],
             "usage": {
@@ -1712,33 +1823,43 @@ class APIHandler(BaseHTTPRequestHandler):
             file_names = {f.file_path.name for f in repo.refs["main"].files}
             return all(f in file_names for f in files)
 
-        # Scan the cache directory for downloaded mlx models
-        hf_cache_info = scan_cache_dir()
-        downloaded_models = [
-            repo for repo in hf_cache_info.repos if probably_mlx_lm(repo)
-        ]
-
-        # Create a list of available models
-        models = [
-            {
-                "id": repo.repo_id,
-                "object": "model",
-                "created": self.created,
-            }
-            for repo in downloaded_models
-        ]
-
-        if self.response_generator.cli_args.model:
-            model_path = Path(self.response_generator.cli_args.model)
-            if model_path.exists():
-                model_id = str(model_path.resolve())
-                models.append(
-                    {
-                        "id": model_id,
-                        "object": "model",
-                        "created": self.created,
-                    }
-                )
+        model_id = getattr(self.response_generator.cli_args, "model_id", None)
+        if model_id:
+            models = [
+                {
+                    "id": model_id,
+                    "object": "model",
+                    "created": self.created,
+                }
+            ]
+        else:
+            # Scan the cache directory for downloaded mlx models
+            hf_cache_info = scan_cache_dir()
+            downloaded_models = [
+                repo for repo in hf_cache_info.repos if probably_mlx_lm(repo)
+            ]
+
+            # Create a list of available models
+            models = [
+                {
+                    "id": repo.repo_id,
+                    "object": "model",
+                    "created": self.created,
+                }
+                for repo in downloaded_models
+            ]
+
+            if self.response_generator.cli_args.model:
+                model_path = Path(self.response_generator.cli_args.model)
+                if model_path.exists():
+                    model_id = str(model_path.resolve())
+                    models.append(
+                        {
+                            "id": model_id,
+                            "object": "model",
+                            "created": self.created,
+                        }
+                    )
 
         response = {"object": "list", "data": models}
 
@@ -1803,6 +1924,11 @@ def main():
         type=str,
         help="The path to the MLX model weights, tokenizer, and config",
     )
+    parser.add_argument(
+        "--model-id",
+        type=str,
+        help="Override the model ID returned by the API",
+    )
     parser.add_argument(
         "--adapter-path",
         type=str,
