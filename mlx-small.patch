diff --git a/mlx_lm/server.py b/mlx_lm/server.py
index 6c69ea7..cffe0c0 100644
--- a/mlx_lm/server.py
+++ b/mlx_lm/server.py
@@ -9,7 +9,6 @@ import socket
 import time
 import uuid
 import warnings
-from collections import deque
 from dataclasses import dataclass
 from http.server import BaseHTTPRequestHandler, ThreadingHTTPServer
 from pathlib import Path
@@ -1088,6 +1087,16 @@ class APIHandler(BaseHTTPRequestHandler):
         self.send_header("Cache-Control", "no-cache")
         self._set_cors_headers()
 
+    def _safe_write(self, payload: bytes, flush: bool = True) -> bool:
+        try:
+            self.wfile.write(payload)
+            if flush:
+                self.wfile.flush()
+            return True
+        except (BrokenPipeError, ConnectionResetError, OSError):
+            logging.info("Client disconnected before response completed")
+            return False
+
     def do_OPTIONS(self):
         self._set_completion_headers(204)
         self.end_headers()
@@ -1105,7 +1114,7 @@ class APIHandler(BaseHTTPRequestHandler):
         if self.path not in request_factories:
             self._set_completion_headers(404)
             self.end_headers()
-            self.wfile.write(b"Not Found")
+            self._safe_write(b"Not Found")
             return
 
         # Fetch and parse request body
@@ -1133,7 +1142,7 @@ class APIHandler(BaseHTTPRequestHandler):
             logging.error(f"JSONDecodeError: {e} - Raw body: {raw_body.decode()}")
             self._set_completion_headers(400)
             self.end_headers()
-            self.wfile.write(
+            self._safe_write(
                 json.dumps({"error": f"Invalid JSON in request body: {e}"}).encode()
             )
             return
@@ -1155,6 +1164,13 @@ class APIHandler(BaseHTTPRequestHandler):
         self.stream = self.body.get("stream", False)
         self.stream_options = self.body.get("stream_options", None)
         self.requested_model = self.body.get("model", "default_model")
+        self.response_model_id = self.requested_model
+        model_id = getattr(self.response_generator.cli_args, "model_id", None)
+        if model_id and self.requested_model == model_id:
+            self.requested_model = (
+                self.response_generator.cli_args.model or "default_model"
+            )
+            self.response_model_id = model_id
         self.requested_draft_model = self.body.get("draft_model", "default_model")
         self.num_draft_tokens = self.body.get(
             "num_draft_tokens", self.response_generator.cli_args.num_draft_tokens
@@ -1193,7 +1209,11 @@ class APIHandler(BaseHTTPRequestHandler):
 
         # Create the completion request
         request = request_factories[self.path]()
-        self.handle_completion(request, stop_words)
+        try:
+            self.handle_completion(request, stop_words)
+        except (BrokenPipeError, ConnectionResetError, OSError):
+            logging.info("Client disconnected while sending response")
+            return
 
     def _validate(
         self,
@@ -1298,7 +1318,7 @@ class APIHandler(BaseHTTPRequestHandler):
             "id": self.request_id,
             "system_fingerprint": self.system_fingerprint,
             "object": self.object_type,
-            "model": self.requested_model,
+            "model": self.response_model_id,
             "created": self.created,
             "choices": [
                 {
@@ -1417,7 +1437,7 @@ class APIHandler(BaseHTTPRequestHandler):
         except Exception as e:
             self._set_completion_headers(404)
             self.end_headers()
-            self.wfile.write(json.dumps({"error": str(e)}).encode())
+            self._safe_write(json.dumps({"error": f"{e}"}).encode())
             return
 
         # Prepare the headers
@@ -1479,8 +1499,9 @@ class APIHandler(BaseHTTPRequestHandler):
                         tool_calls=tool_formatter(tool_calls),
                         reasoning_text=reasoning_text,
                     )
-                    self.wfile.write(f"data: {json.dumps(resp)}\n\n".encode())
-                    self.wfile.flush()
+                    if not self._safe_write(f"data: {json.dumps(resp)}\n\n".encode()):
+                        ctx.stop()
+                        return
                     reasoning_text = ""
                     text = ""
                     tool_calls = []
@@ -1504,8 +1525,9 @@ class APIHandler(BaseHTTPRequestHandler):
                     tool_calls=tool_formatter(tool_calls),
                     reasoning_text=reasoning_text,
                 )
-                self.wfile.write(f"data: {json.dumps(resp)}\n\n".encode())
-                self.wfile.flush()
+                if not self._safe_write(f"data: {json.dumps(resp)}\n\n".encode()):
+                    ctx.stop()
+                    return
                 if (
                     self.stream_options is not None
                     and self.stream_options["include_usage"]
@@ -1515,10 +1537,12 @@ class APIHandler(BaseHTTPRequestHandler):
                         len(tokens),
                         ctx.prompt_cache_count,
                     )
-                    self.wfile.write(f"data: {json.dumps(resp)}\n\n".encode())
-                    self.wfile.flush()
-                self.wfile.write("data: [DONE]\n\n".encode())
-                self.wfile.flush()
+                    if not self._safe_write(f"data: {json.dumps(resp)}\n\n".encode()):
+                        ctx.stop()
+                        return
+                if not self._safe_write("data: [DONE]\n\n".encode()):
+                    ctx.stop()
+                    return
             else:
                 resp = self.generate_response(
                     text,
@@ -1539,8 +1563,8 @@ class APIHandler(BaseHTTPRequestHandler):
                 response_json = json.dumps(resp).encode()
                 self.send_header("Content-Length", str(len(response_json)))
                 self.end_headers()
-                self.wfile.write(response_json)
-                self.wfile.flush()
+                if not self._safe_write(response_json):
+                    return
         finally:
             ctx.stop()
 
@@ -1554,7 +1578,7 @@ class APIHandler(BaseHTTPRequestHandler):
             "id": self.request_id,
             "system_fingerprint": self.system_fingerprint,
             "object": "chat.completion",
-            "model": self.requested_model,
+            "model": self.response_model_id,
             "created": self.created,
             "choices": [],
             "usage": {
@@ -1621,7 +1645,7 @@ class APIHandler(BaseHTTPRequestHandler):
         else:
             self._set_completion_headers(404)
             self.end_headers()
-            self.wfile.write(b"Not Found")
+            self._safe_write(b"Not Found")
 
     def handle_health_check(self):
         """
@@ -1630,8 +1654,7 @@ class APIHandler(BaseHTTPRequestHandler):
         self._set_completion_headers(200)
         self.end_headers()
 
-        self.wfile.write('{"status": "ok"}'.encode())
-        self.wfile.flush()
+        self._safe_write('{"status": "ok"}'.encode())
 
     def handle_models_request(self):
         """
@@ -1657,39 +1680,48 @@ class APIHandler(BaseHTTPRequestHandler):
             file_names = {f.file_path.name for f in repo.refs["main"].files}
             return all(f in file_names for f in files)
 
-        # Scan the cache directory for downloaded mlx models
-        hf_cache_info = scan_cache_dir()
-        downloaded_models = [
-            repo for repo in hf_cache_info.repos if probably_mlx_lm(repo)
-        ]
-
-        # Create a list of available models
-        models = [
-            {
-                "id": repo.repo_id,
-                "object": "model",
-                "created": self.created,
-            }
-            for repo in downloaded_models
-        ]
-
-        if self.response_generator.cli_args.model:
-            model_path = Path(self.response_generator.cli_args.model)
-            if model_path.exists():
-                model_id = str(model_path.resolve())
-                models.append(
-                    {
-                        "id": model_id,
-                        "object": "model",
-                        "created": self.created,
-                    }
-                )
+        model_id = getattr(self.response_generator.cli_args, "model_id", None)
+        if model_id:
+            models = [
+                {
+                    "id": model_id,
+                    "object": "model",
+                    "created": self.created,
+                }
+            ]
+        else:
+            # Scan the cache directory for downloaded mlx models
+            hf_cache_info = scan_cache_dir()
+            downloaded_models = [
+                repo for repo in hf_cache_info.repos if probably_mlx_lm(repo)
+            ]
+
+            # Create a list of available models
+            models = [
+                {
+                    "id": repo.repo_id,
+                    "object": "model",
+                    "created": self.created,
+                }
+                for repo in downloaded_models
+            ]
+
+            if self.response_generator.cli_args.model:
+                model_path = Path(self.response_generator.cli_args.model)
+                if model_path.exists():
+                    model_id = str(model_path.resolve())
+                    models.append(
+                        {
+                            "id": model_id,
+                            "object": "model",
+                            "created": self.created,
+                        }
+                    )
 
         response = {"object": "list", "data": models}
 
         response_json = json.dumps(response).encode()
-        self.wfile.write(response_json)
-        self.wfile.flush()
+        self._safe_write(response_json)
 
 
 def _run_http_server(
@@ -1748,6 +1780,11 @@ def main():
         type=str,
         help="The path to the MLX model weights, tokenizer, and config",
     )
+    parser.add_argument(
+        "--model-id",
+        type=str,
+        help="Override the model ID returned by the API",
+    )
     parser.add_argument(
         "--adapter-path",
         type=str,
